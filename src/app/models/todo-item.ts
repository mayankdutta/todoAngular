export interface iTodoItem {
  id: number;

  title: string;
  description: string;
  dueDate: Date;
  priority: string;
  status: string;

  createdAt: Date;
  updatedAt: Date;


}

        // if (elem1.id < elem2.id) {
        //   return -1;
        // } else if (elem1.id > elem2.id) {
        //   return 1;
        // } else {
        //   return 0;
        // }
